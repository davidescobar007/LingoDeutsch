declare module 'html-to-json-parser' {
   // Define types according to the module's API
   export function HTMLToJSON(_html: string, _isObject?: boolean): any
   // Add more functions/types as needed
}
